# Context Changelog

Each entry corresponds to a snapshot under context/.snapshots/v<version>/.
Bump via `python3 tools/context_snapshot.py "reason"`.

## v0.1.0 -- 2026-04-14

- Initial versioning of agency-default context files.

